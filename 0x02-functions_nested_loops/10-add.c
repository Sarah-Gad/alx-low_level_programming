#include "main.h"
/**
 * add - a function that adds two
 * integers and returns the result.
 * @a: the first input
 * @d: the second input
 *
 * Return: sum
*/

int add(int a, int d)
{
	int sum = a + d;

	return (sum);
}
