#include "main.h"
/**
 * mul -  a function that
 * multiplies two integers.
 *
 * @f_num: input
 * @s_num: input
 *
 * Return: (mul)
*/

int mul(int f_num, int s_num)
{
	int mul;

	mul = f_num * s_num;

	return (mul);
}
